---
layout: ../../../layouts/CourseLayout.astro
title: "Section 6.2 • The Binary Branch"
description: "Controlling program flow with if/else — the fork in the road"
---

import Checkpoint from '../../../components/Checkpoint.astro';
import TryItNowRunner from '../../../components/TryItNowRunner.astro';
import AnnotatedCode from '../../../components/AnnotatedCode.astro';

# Section 6.2: The Binary Branch (if/else)

<div class="section-header">
  <div class="section-badge">🔀 The Logic Diverter</div>
  <p class="section-time">⏱️ Estimated Time: 30 minutes</p>
</div>

---

<div class="concept-header">
  <div class="header-content">
    <div class="header-text">
      <h2>✦ The Logic Diverter: Choosing a Path</h2>
      <p>
        Imagine a mechanical rail switch at a train junction. A data packet arrives, the switch evaluates a condition, and the packet is diverted down <strong>Path A</strong> or <strong>Path B</strong>. There is no third option. The condition is either true or false, and the program follows exactly one path. This is the <code>if/else</code> statement — the most fundamental decision structure in all of programming.
      </p>
    </div>
    <div class="header-image">
      <img src="/media/week%206/6.2/binaryBranch.jpeg" alt="Binary branch concept — choosing between two paths" />
    </div>
  </div>
</div>

---

## 🎬 Technical Briefing: The if/else Structure

<div class="video-section">
  <p class="video-intro">Watch Part 1 of this briefing on the <code>if</code> statement in C# — basic structure, block behavior, and how the program chooses a path.</p>
  <div class="video-container">
    <iframe 
      src="https://www.youtube.com/embed/pX-SvbIq2Gs" 
      title="If/Else in C# — Part 1"
      frameborder="0" 
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" 
      referrerpolicy="strict-origin-when-cross-origin"
      allowfullscreen>
    </iframe>
  </div>
</div>

---

## 🧠 The if Statement

The `if` statement executes a block of code **only when the condition is true**:

```csharp
if (condition) {
    // This code runs ONLY if condition is true
}
```

### 🖱️ Hover to Explore: The if Statement

<AnnotatedCode
  title="Anatomy of an if Statement"
  tokens={[
    { text: "if", type: "keyword", tooltip: "The 'if' keyword starts a conditional check. The program decides whether to run the next block." },
    { text: " (", type: "punctuation" },
    { text: "score >= 70", type: "parameter", tooltip: "The condition — must evaluate to true or false. This checks: 'is score greater than or equal to 70?'" },
    { text: ") {", type: "punctuation", tooltip: "Opening brace on same line = K&R style. Everything between { and } is the conditional block." },
    { text: " ... ", type: "plain" },
    { text: "}", type: "punctuation", tooltip: "Closing brace ends the conditional block. Code after this runs regardless of the condition." }
  ]}
/>

<div class="architect-rule">

#### 🏗️ Architect's Rule: K&R Brace Style

In this course, we use **K&R style** (also called "Egyptian braces") — the opening brace goes on the **same line** as the statement. This is the industry standard for many teams and keeps code compact.

```csharp
// ✅ K&R Style (our standard)
if (score >= 70) {
    Console.WriteLine("Passing!");
}

// ❌ Allman Style (NOT our standard)
if (score >= 70)
{
    Console.WriteLine("Passing!");
}
```

</div>

---

## 🔀 The if/else Fork

When you need your program to do **one thing or another**, use `if/else`:

```csharp
int score = 85;

if (score >= 70) {
    Console.WriteLine("You passed! ✅");
} else {
    Console.WriteLine("Keep practicing. ❌");
}
```

Think of it as a **fork in the road**: the program MUST take one path or the other. It can never take both.

<img src="/media/week%206/6.2/logicDiverter.jpeg" alt="If/else fork diagram — program takes one path or the other" style="max-width: 100%; border-radius: 12px; margin: 24px 0;" />

---

## 🔬 Try It: The Fork in the Road

<TryItNowRunner 
  starterId="week-06-lesson-1" 
  height={400}
  enableInteractiveTerminal={true}
  code={`using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter your age: ");
        string ageText = Console.ReadLine();
        int age = int.Parse(ageText);
        
        if (age >= 18) {
            Console.WriteLine("ACCESS GRANTED: You are an adult.");
            Console.WriteLine("You can vote, sign contracts, and serve.");
        } else {
            Console.WriteLine("ACCESS RESTRICTED: You are a minor.");
            int yearsLeft = 18 - age;
            Console.WriteLine($"You must wait {yearsLeft} more year(s).");
        }
        
        Console.WriteLine("\\nProgram complete.");
    }
}`}
  instructions="Enter different ages to see how the program chooses different paths. Notice how code AFTER the if/else always runs." 
/>

<div class="experiment-box">
  <h4>🧪 Experiment</h4>
  <ol>
    <li>Enter age <code>25</code> — you get the "adult" path</li>
    <li>Enter age <code>14</code> — you get the "minor" path with years countdown</li>
    <li>Enter age <code>18</code> — which path? The condition is <code>&gt;=</code> so 18 counts!</li>
    <li>Notice <code>"Program complete."</code> ALWAYS prints — it's outside the if/else</li>
  </ol>
</div>

---

## 🔄 Execution Flow: How if/else Works

<div class="flow-diagram">
  <div class="flow-step">
    <span class="flow-number">1</span>
    <div class="flow-content">
      <strong>Evaluate Condition</strong>
      <p>The expression inside <code>()</code> is checked</p>
    </div>
  </div>
  <div class="flow-arrow">↓</div>
  <div class="flow-step">
    <span class="flow-number">2</span>
    <div class="flow-content">
      <strong>Condition is true?</strong>
      <p>Execute the <code>if</code> block, skip the <code>else</code> block</p>
    </div>
  </div>
  <div class="flow-arrow">↓</div>
  <div class="flow-step">
    <span class="flow-number">3</span>
    <div class="flow-content">
      <strong>Condition is false?</strong>
      <p>Skip the <code>if</code> block, execute the <code>else</code> block</p>
    </div>
  </div>
  <div class="flow-arrow">↓</div>
  <div class="flow-step">
    <span class="flow-number">4</span>
    <div class="flow-content">
      <strong>Continue</strong>
      <p>Code after the if/else runs no matter what</p>
    </div>
  </div>
</div>

---

## 🎯 The Code Block: Braces Matter

Everything between `{` and `}` is a **code block**. Only the code inside the matching block executes:

```csharp
if (temperature > 100) {
    // ALL of these lines run together (or none of them)
    Console.WriteLine("WARNING: Overheating!");
    Console.WriteLine("Shutting down systems...");
    Console.WriteLine("Emergency cooling activated.");
}
// This line ALWAYS runs, regardless of temperature
Console.WriteLine("System check complete.");
```

<div class="architect-rule">

#### ⚠️ Common Bug: Missing Braces

Without braces, only the **first line** after `if` is conditional. The rest always runs. **Always use braces**, even for single-line blocks.

```csharp
// ❌ DANGEROUS — only the first line is conditional!
if (isAdmin)
    Console.WriteLine("Welcome, Admin.");
    Console.WriteLine("Loading admin panel...");  // ALWAYS runs!

// ✅ SAFE — both lines are inside the block
if (isAdmin) {
    Console.WriteLine("Welcome, Admin.");
    Console.WriteLine("Loading admin panel...");
}
```

</div>

---

## 🔬 Try It: Nested Code Blocks

Variables declared inside an `if` block only exist within that block (**scope**):

<TryItNowRunner 
  starterId="week-06-lesson-1" 
  height={400}
  enableInteractiveTerminal={true}
  code={`using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter a number: ");
        int number = int.Parse(Console.ReadLine());
        
        if (number > 0) {
            string status = "positive";
            Console.WriteLine($"{number} is {status}.");
            Console.WriteLine($"Double: {number * 2}");
        } else {
            string status = "zero or negative";
            Console.WriteLine($"{number} is {status}.");
            Console.WriteLine($"Absolute value: {Math.Abs(number)}");
        }
        
        // 'status' doesn't exist here — it was declared inside the blocks
        Console.WriteLine("Analysis complete.");
    }
}`}
  instructions="Try entering positive numbers, zero, and negative numbers. Each path has its own 'status' variable that only exists within its block." 
/>

---

## 🔍 Checking Multiple Things in Sequence

You can have multiple independent `if` statements. Each one is checked separately:

```csharp
int score = 85;

// These are INDEPENDENT checks — each runs on its own
if (score >= 70) {
    Console.WriteLine("✅ Passing grade");
}

if (score >= 90) {
    Console.WriteLine("🌟 Honor roll!");
}

if (score == 100) {
    Console.WriteLine("🏆 Perfect score!");
}
```

With a score of 85, only the first message prints. With 95, the first two print. With 100, all three print. Each `if` is a separate decision.

---

## 🧩 Predict the Output Challenge

<div class="predict-challenge">
  <h4>🔮 What prints when x = 7?</h4>
  <div class="predict-code">

```csharp
int x = 7;

if (x > 10) {
    Console.WriteLine("Big");
} else {
    Console.WriteLine("Small");
}

Console.WriteLine("Done");
```

  </div>
  <details class="predict-reveal">
    <summary>Click to reveal the answer</summary>
    <div class="predict-answer">
      <p><code>Small</code></p>
      <p><code>Done</code></p>
      <p><strong>Why?</strong> 7 is NOT greater than 10, so the <code>else</code> block runs. Then <code>"Done"</code> prints because it's <em>outside</em> the if/else — it always runs.</p>
    </div>
  </details>
</div>

---

<Checkpoint
  checkpointId="week-06-6-2-checkpoint"
  title="🔍 Section 6.2 Knowledge Check"
  questions={[
    {
      id: "cp1",
      type: "multiple-choice",
      question: "What happens if you write an if statement WITHOUT braces and put two lines after it?",
      options: ["Both lines are conditional", "Only the first line is conditional — the second always runs", "Neither line runs", "The compiler throws an error"],
      correctAnswer: "Only the first line is conditional — the second always runs",
      explanation: "Without braces, only the FIRST line after if is conditional. The second line always executes regardless of the condition. Always use braces to avoid this bug!"
    },
    {
      id: "cp2",
      type: "multiple-choice",
      question: "In an if/else block, how many paths will the program take?",
      options: ["Both paths always execute", "Neither path — it skips to the next line", "Exactly one path — either if or else, never both", "It depends on the data type"],
      correctAnswer: "Exactly one path — either if or else, never both",
      explanation: "An if/else is a binary fork — the program MUST take exactly one path. If the condition is true, it takes the if path. If false, it takes the else path. Never both, never neither."
    }
  ]}
/>

---

<div class="next-section">
  <p>Next: Learn to combine multiple conditions with <strong>logical operators</strong>!</p>
  <a href="/week-06/6-3-logical-operators/" class="vscode-button">Continue to 6.3: Logical Combinators →</a>
</div>

<style>{`
  .architect-rule {
    background: rgba(239, 68, 68, 0.1);
    border: 2px solid #ef4444;
    border-radius: 12px;
    padding: 1.5rem;
    margin: 1.5rem 0;
  }
  
  .architect-rule h4 {
    color: #fca5a5;
    margin-top: 0;
  }
  
  .architect-rule p {
    color: #e2e8f0;
  }
  
  .next-section {
    background: linear-gradient(135deg, rgba(78, 201, 176, 0.15) 0%, rgba(107, 159, 255, 0.15) 100%);
    border: 2px solid #4ec9b0;
    border-radius: 12px;
    padding: 24px;
    text-align: center;
    margin: 32px 0;
  }
  
  .next-section p { margin: 8px 0; color: rgba(255, 255, 255, 0.9); }

  body[data-theme="light"] .architect-rule { background: rgba(239, 68, 68, 0.05) !important; }
  body[data-theme="light"] .architect-rule h4 { color: #dc2626 !important; }
  body[data-theme="light"] .architect-rule p { color: #334155 !important; }
  body[data-theme="light"] .next-section { background: linear-gradient(135deg, rgba(78, 201, 176, 0.1) 0%, rgba(107, 159, 255, 0.1) 100%) !important; }
  body[data-theme="light"] .next-section p { color: #333 !important; }

  .experiment-box {
    background: linear-gradient(135deg, #1e2a1e 0%, #1a2a1a 100%);
    border: 1px solid #4ec9b0;
    border-radius: 12px;
    padding: 20px;
    margin: 20px 0;
  }
  .experiment-box h4 { margin-top: 0; color: #4ec9b0; }
  .experiment-box ol { margin-bottom: 0; }
  body[data-theme="light"] .experiment-box { background: linear-gradient(135deg, #e8f8e8 0%, #e0f0e0 100%) !important; }
  body[data-theme="light"] .experiment-box h4 { color: #0a7c62 !important; }
  body[data-theme="light"] .experiment-box li { color: #333 !important; }
  body[data-theme="light"] .experiment-box code { background: rgba(78, 201, 176, 0.2) !important; color: #0a7c62 !important; }

  .flow-diagram {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    margin: 24px 0;
    padding: 24px;
    background: #0d1117;
    border-radius: 12px;
  }
  .flow-step {
    display: flex;
    align-items: center;
    gap: 16px;
    background: #1e1e2e;
    padding: 16px 24px;
    border-radius: 8px;
    width: 100%;
    max-width: 400px;
  }
  .flow-number {
    background: #4ec9b0;
    color: #000;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    flex-shrink: 0;
  }
  .flow-content strong { display: block; color: #e0e0e0; }
  .flow-content p { margin: 4px 0 0; color: #888; font-size: 0.9rem; }
  .flow-arrow { color: #4ec9b0; font-size: 1.5rem; }
  body[data-theme="light"] .flow-diagram { background: #f5f5f5 !important; border: 1px solid #ddd; }
  body[data-theme="light"] .flow-step { background: #fff !important; border: 1px solid #ddd; }
  body[data-theme="light"] .flow-content strong { color: #333 !important; }
  body[data-theme="light"] .flow-content p { color: #555 !important; }

  .predict-challenge {
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
    border: 2px solid #f0ad4e;
    border-radius: 12px;
    padding: 24px;
    margin: 24px 0;
  }
  .predict-challenge h4 { color: #f0ad4e; margin-top: 0; }
  .predict-code { margin: 16px 0; }
  .predict-reveal { margin-top: 16px; }
  .predict-reveal summary {
    cursor: pointer;
    color: #4ec9b0;
    font-weight: 600;
    padding: 12px 16px;
    background: rgba(78, 201, 176, 0.1);
    border-radius: 8px;
  }
  .predict-reveal summary:hover { background: rgba(78, 201, 176, 0.2); }
  .predict-answer {
    margin-top: 12px;
    padding: 16px;
    background: rgba(78, 201, 176, 0.05);
    border-radius: 8px;
    border-left: 3px solid #4ec9b0;
  }
  .predict-answer p { margin: 6px 0; }
  body[data-theme="light"] .predict-challenge { background: linear-gradient(135deg, #fefce8 0%, #fef3c7 100%) !important; }
  body[data-theme="light"] .predict-answer p { color: #333 !important; }
`}</style>
