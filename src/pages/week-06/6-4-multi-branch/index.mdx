---
layout: ../../../layouts/CourseLayout.astro
title: "Section 6.4 • The Multi-Branch"
description: "Managing complex decisions with else if chains"
---

import Checkpoint from '../../../components/Checkpoint.astro';
import TryItNowRunner from '../../../components/TryItNowRunner.astro';
import AnnotatedCode from '../../../components/AnnotatedCode.astro';
import DeepDiveTabs from '../../../components/DeepDiveTabs.astro';

# Section 6.4: The Multi-Branch (else if)

<div class="section-header">
  <div class="section-badge">🗼 The Control Tower</div>
  <p class="section-time">⏱️ Estimated Time: 30 minutes</p>
</div>

---

<div class="concept-header">
  <div class="header-content">
    <div class="header-text">
      <h2>✦ The Control Tower: Managing Multiple Flight Paths</h2>
      <p>
        An air traffic control tower doesn't just say "land" or "don't land." It evaluates altitude, weather, runway availability, and fuel — then routes each aircraft to the appropriate action. The <code>else if</code> chain works the same way: it checks conditions <strong>in order</strong>, and the <em>first</em> match wins. Everything below is skipped. This is how you handle decisions with <strong>more than two outcomes</strong>.
      </p>
    </div>
    <div class="header-image">
      <img src="/media/week%206/6.4/controlTower.jpeg" alt="Control tower concept — managing multiple decision paths" />
    </div>
  </div>
</div>

---

## 🎬 Technical Briefing: The else if Structure

<div class="video-section">
  <p class="video-intro">Watch Part 2 of the if statement briefing — multi-choice logic using <code>else if</code> chains to handle decisions with more than two outcomes.</p>
  <div class="video-container">
    <iframe 
      src="https://www.youtube.com/embed/pX-SvbIq2Gs" 
      title="If/Else in C# — Part 2: else if Chains"
      frameborder="0" 
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" 
      referrerpolicy="strict-origin-when-cross-origin"
      allowfullscreen>
    </iframe>
  </div>
</div>

---

## 🔗 The else if Chain

When you have more than two possible outcomes, chain multiple conditions together:

```csharp
int score = 85;

if (score >= 90) {
    Console.WriteLine("Grade: A");
} else if (score >= 80) {
    Console.WriteLine("Grade: B");
} else if (score >= 70) {
    Console.WriteLine("Grade: C");
} else if (score >= 60) {
    Console.WriteLine("Grade: D");
} else {
    Console.WriteLine("Grade: F");
}
```

### How the Chain Works

```
  score = 85

  Is score >= 90? → NO (85 < 90)  ──→ skip
  Is score >= 80? → YES (85 ≥ 80) ──→ ✅ "Grade: B"
  Is score >= 70? → NEVER CHECKED (already matched)
  Is score >= 60? → NEVER CHECKED
  else            → NEVER CHECKED
```

**Critical rule**: The chain stops at the **first true condition**. Order matters!

<AnnotatedCode
  title="🔬 Anatomy of an else if Chain"
  tokens={[
    { text: "if", type: "keyword", tooltip: "First check — most restrictive condition tested first" },
    { text: " (", type: "punctuation" },
    { text: "score >= 90", type: "parameter", tooltip: "Most restrictive condition — only scores 90+ pass this gate" },
    { text: ") { ... }", type: "punctuation" },
    { text: " " },
    { text: "else if", type: "keyword", tooltip: "Only checked when ALL previous conditions were false" },
    { text: " (", type: "punctuation" },
    { text: "score >= 80", type: "parameter", tooltip: "We already know score < 90, so this catches 80-89" },
    { text: ") { ... }", type: "punctuation" },
    { text: " " },
    { text: "else", type: "keyword", tooltip: "The safety net — catches everything that failed ALL conditions above" },
    { text: " { ... }", type: "punctuation" }
  ]}
/>

<div class="flow-diagram">
  <h4>🔄 How First-Match-Wins Works</h4>
  <div class="flow-steps">
    <div class="flow-step">
      <div class="flow-number">1</div>
      <div>Check <code>score &gt;= 90</code> — if true → run block → <strong>STOP</strong></div>
    </div>
    <div class="flow-arrow">↓ false</div>
    <div class="flow-step">
      <div class="flow-number">2</div>
      <div>Check <code>score &gt;= 80</code> — if true → run block → <strong>STOP</strong></div>
    </div>
    <div class="flow-arrow">↓ false</div>
    <div class="flow-step">
      <div class="flow-number">3</div>
      <div>Check <code>score &gt;= 70</code> — if true → run block → <strong>STOP</strong></div>
    </div>
    <div class="flow-arrow">↓ false</div>
    <div class="flow-step">
      <div class="flow-number">4</div>
      <div><code>else</code> block runs — the final safety net catches everything left</div>
    </div>
  </div>
  <p style="margin-top: 1rem; opacity: 0.8; font-style: italic;">💡 Only ONE block ever executes. The chain exits the moment it finds a match.</p>
</div>

---

## ⚠️ Order Matters: A Common Trap

If you check the **least restrictive** condition first, it catches everything:

```csharp
int score = 95;

// ❌ WRONG ORDER — score >= 60 catches everything above 60!
if (score >= 60) {
    Console.WriteLine("Grade: D");  // A 95 gets "D"!?
} else if (score >= 70) {
    Console.WriteLine("Grade: C");  // Never reached
} else if (score >= 80) {
    Console.WriteLine("Grade: B");  // Never reached
} else if (score >= 90) {
    Console.WriteLine("Grade: A");  // Never reached
}
```

<div class="architect-rule">
  <h4>🏗️ Architect's Rule: Most Restrictive First</h4>
  <p>Always order <code>else if</code> chains from <strong>most restrictive to least restrictive</strong>. Check for 90+ before 80+, 80+ before 70+, and so on. The first <code>true</code> condition wins — everything below is dead code if you get the order wrong.</p>
</div>

---

## 🔬 Try It: Grade Calculator

<TryItNowRunner 
  starterId="week-06-lesson-1" 
  height={450}
  enableInteractiveTerminal={true}
  code={`using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter your score (0-100): ");
        int score = int.Parse(Console.ReadLine());
        
        string grade;
        string message;
        
        if (score >= 90) {
            grade = "A";
            message = "Outstanding work!";
        } else if (score >= 80) {
            grade = "B";
            message = "Great job!";
        } else if (score >= 70) {
            grade = "C";
            message = "Satisfactory.";
        } else if (score >= 60) {
            grade = "D";
            message = "Needs improvement.";
        } else {
            grade = "F";
            message = "Please see the instructor.";
        }
        
        Console.WriteLine($"Score: {score}");
        Console.WriteLine($"Grade: {grade}");
        Console.WriteLine($"Feedback: {message}");
    }
}`}
  instructions="Enter different scores to see the grade calculation. Try 95, 85, 72, 65, and 40. Notice that only ONE grade is ever assigned." 
/>

<div class="experiment-box">

#### 🧪 Experiment: The Order Trap

Try these modifications in the grade calculator above to see order-of-conditions in action:

1. **Enter 95** — You get an A. Now swap the first condition from `>= 90` to `>= 60`. What grade does 95 get now?
2. **Enter 80** — You get a B. What if you move the `>= 80` check to be the FIRST condition?
3. **Enter 59** — You get an F. What if you remove the `else` block entirely? (Hint: the variable won't be assigned!)
4. **Try boundary values**: 90, 80, 70, 60 — each sits right on the edge. Which block catches them?

💡 **Discovery**: The order of conditions is the difference between correct and completely broken code!

</div>

<DeepDiveTabs
  title="📊 else if vs. Separate ifs"
  tabs={[
    {
      id: "chain",
      label: "🔗 else if Chain",
      content: "An else if chain is EXCLUSIVE — only ONE block runs. Once a condition matches, all remaining conditions are skipped. Use this when your outcomes are mutually exclusive: a score can only be ONE grade letter.",
      keyPoints: ["First match wins", "All remaining conditions skipped", "Exactly ONE block executes"]
    },
    {
      id: "separate",
      label: "📋 Separate ifs",
      content: "Separate if statements are INDEPENDENT — each one is checked regardless of previous results. A temperature of -10 could trigger BOTH a 'freezing warning' AND a 'dangerous cold alert' if they're separate ifs.",
      keyPoints: ["Every condition is checked", "Multiple blocks can run", "No 'first match wins' behavior"]
    },
    {
      id: "choose",
      label: "🤔 How to Choose",
      content: "Ask yourself: 'Can multiple conditions be true at the same time, and should they all execute?' If YES → separate ifs. If only ONE should execute → else if chain. Getting this wrong is a common bug!",
      keyPoints: ["Mutually exclusive? → else if", "Independent checks? → separate ifs", "Wrong choice = subtle bugs"]
    }
  ]}
/>

---

## 🚫 Avoiding Nesting Hell

Beginners often nest `if` statements deep. This creates hard-to-read "arrow code":

```csharp
// ❌ NESTING HELL — hard to read and maintain
if (age >= 18) {
    if (hasID) {
        if (!isExpired) {
            Console.WriteLine("Entry granted.");
        } else {
            Console.WriteLine("ID is expired.");
        }
    } else {
        Console.WriteLine("No ID.");
    }
} else {
    Console.WriteLine("Too young.");
}
```

Flatten with logical operators and early returns:

```csharp
// ✅ FLAT AND CLEAR — same logic, much easier to read
if (age < 18) {
    Console.WriteLine("Too young.");
} else if (!hasID) {
    Console.WriteLine("No ID.");
} else if (isExpired) {
    Console.WriteLine("ID is expired.");
} else {
    Console.WriteLine("Entry granted.");
}
```

---

## 🔬 Try It: The Control Tower

<TryItNowRunner 
  starterId="week-06-lesson-1" 
  height={500}
  enableInteractiveTerminal={true}
  code={`using System;

class Program
{
    static void Main()
    {
        Console.Write("Enter altitude (feet): ");
        int altitude = int.Parse(Console.ReadLine());
        
        Console.Write("Is weather clear? (true/false): ");
        bool isClear = bool.Parse(Console.ReadLine());
        
        Console.WriteLine("\\n--- CONTROL TOWER REPORT ---");
        Console.WriteLine($"Altitude: {altitude} ft");
        Console.WriteLine($"Weather: {(isClear ? "Clear" : "Overcast")}");
        Console.WriteLine();
        
        if (altitude <= 0) {
            Console.WriteLine("STATUS: On the ground.");
            Console.WriteLine("ACTION: Taxi to gate.");
        } else if (altitude < 1000 && !isClear) {
            Console.WriteLine("STATUS: ⚠️ DANGER ZONE");
            Console.WriteLine("ACTION: Abort landing — go around!");
        } else if (altitude < 1000) {
            Console.WriteLine("STATUS: Final approach.");
            Console.WriteLine("ACTION: Landing gear down. Clear to land.");
        } else if (altitude < 10000) {
            Console.WriteLine("STATUS: Descent phase.");
            Console.WriteLine("ACTION: Begin approach procedures.");
        } else if (altitude < 30000) {
            Console.WriteLine("STATUS: Cruising altitude.");
            Console.WriteLine("ACTION: Maintain heading.");
        } else {
            Console.WriteLine("STATUS: High altitude.");
            Console.WriteLine("ACTION: Monitor cabin pressure.");
        }
    }
}`}
  instructions="Try different altitudes and weather conditions. Notice how conditions combine altitude AND weather, and order matters for correct routing." 
/>

---

## 🔍 When to Use What

| Situation | Use |
|-----------|-----|
| Yes or no decision | `if` / `if-else` |
| Multiple exclusive outcomes | `else if` chain |
| Multiple independent checks | Separate `if` statements |
| Complex combined conditions | `&&`, `\|\|`, `!` |

---

<div class="predict-challenge">

#### 🎯 Predict the Output

What does this code print? Think carefully about which condition matches first:

```csharp
int temp = 75;

if (temp > 100) {
    Console.WriteLine("Boiling!");
} else if (temp > 70) {
    Console.WriteLine("Warm");
} else if (temp > 50) {
    Console.WriteLine("Cool");
} else {
    Console.WriteLine("Cold");
}
Console.WriteLine("Done checking.");
```

<details class="predict-reveal">
<summary>Reveal Answer</summary>
<div class="predict-answer">

**Output:**
```
Warm
Done checking.
```

- `temp > 100`? 75 > 100 → **false** → skip
- `temp > 70`? 75 > 70 → **true** → prints "Warm" → **chain exits**
- `temp > 50`? **NEVER CHECKED** (chain already matched)
- "Done checking." always prints — it's **outside** the chain

🔑 **Key takeaway**: Even though 75 > 50 is also true, it's never evaluated because the chain already found its match at `temp > 70`.
</div>
</details>
</div>

---

<Checkpoint
  checkpointId="week-06-6-4-checkpoint"
  title="🔍 Section 6.4 Knowledge Check"
  questions={[
    {
      id: "cp1",
      type: "multiple-choice",
      question: "In an else if chain, what happens when the FIRST condition is true?",
      options: ["All conditions are still checked", "Only that block runs — all remaining conditions are skipped", "The else block also runs", "The program restarts the chain"],
      correctAnswer: "Only that block runs — all remaining conditions are skipped",
      explanation: "In an else if chain, the FIRST true condition wins. Its block executes, and all remaining else if / else blocks are completely skipped. Order matters!"
    },
    {
      id: "cp2",
      type: "multiple-choice",
      question: "You're writing a grade calculator with else if. What should the FIRST condition check?",
      options: ["The lowest grade (score >= 60) — to catch most students", "The highest grade (score >= 90) — most restrictive first", "The middle grade (score >= 70) — to split evenly", "It doesn't matter — the order has no effect"],
      correctAnswer: "The highest grade (score >= 90) — most restrictive first",
      explanation: "Always check the MOST restrictive condition first. If you check score >= 60 first, it catches ALL passing scores (including 95), and the higher grade checks never execute."
    }
  ]}
/>

---

<div class="next-section">
  <p>Ready to apply everything you've learned? Build <strong>The Security Gatekeeper</strong>!</p>
  <a href="/week-06/lab/" class="vscode-button">Continue to Lab: The Security Gatekeeper →</a>
</div>

<style>{`
  .architect-rule {
    background: rgba(239, 68, 68, 0.1);
    border: 2px solid #ef4444;
    border-radius: 12px;
    padding: 1.5rem;
    margin: 1.5rem 0;
  }
  
  .architect-rule h4 { color: #fca5a5; margin-top: 0; }
  .architect-rule p { color: #e2e8f0; }

  .next-section {
    background: linear-gradient(135deg, rgba(78, 201, 176, 0.15) 0%, rgba(107, 159, 255, 0.15) 100%);
    border: 2px solid #4ec9b0;
    border-radius: 12px;
    padding: 24px;
    text-align: center;
    margin: 32px 0;
  }
  
  .next-section p { margin: 8px 0; color: rgba(255, 255, 255, 0.9); }

  body[data-theme="light"] .architect-rule { background: rgba(239, 68, 68, 0.05) !important; }
  body[data-theme="light"] .architect-rule h4 { color: #dc2626 !important; }
  body[data-theme="light"] .architect-rule p { color: #334155 !important; }
  body[data-theme="light"] .next-section { background: linear-gradient(135deg, rgba(78, 201, 176, 0.1) 0%, rgba(107, 159, 255, 0.1) 100%) !important; }
  body[data-theme="light"] .next-section p { color: #333 !important; }

  .experiment-box {
    background: rgba(16, 185, 129, 0.1);
    border: 2px solid #10b981;
    border-radius: 12px;
    padding: 1.5rem;
    margin: 1.5rem 0;
  }
  .experiment-box h4 { color: #34d399; margin-top: 0; }

  .predict-challenge {
    background: rgba(245, 158, 11, 0.1);
    border: 2px solid #f59e0b;
    border-radius: 12px;
    padding: 1.5rem;
    margin: 1.5rem 0;
  }
  .predict-challenge h4 { color: #fbbf24; margin-top: 0; }
  .predict-reveal { margin-top: 1rem; cursor: pointer; }
  .predict-reveal summary { color: #fbbf24; font-weight: 600; padding: 8px 0; }
  .predict-answer {
    background: rgba(0, 0, 0, 0.2);
    border-radius: 8px;
    padding: 1rem;
    margin-top: 0.5rem;
  }

  .flow-diagram {
    background: rgba(99, 102, 241, 0.1);
    border: 2px solid #818cf8;
    border-radius: 12px;
    padding: 1.5rem;
    margin: 1.5rem 0;
  }
  .flow-diagram h4 { color: #a5b4fc; margin-top: 0; }
  .flow-steps { display: flex; flex-direction: column; gap: 4px; }
  .flow-step {
    display: flex; align-items: center; gap: 12px;
    background: rgba(255,255,255,0.05); border-radius: 8px; padding: 10px 14px;
  }
  .flow-number {
    background: #818cf8; color: #1e1b4b; font-weight: 700;
    border-radius: 50%; width: 28px; height: 28px;
    display: flex; align-items: center; justify-content: center; flex-shrink: 0;
  }
  .flow-arrow { text-align: center; color: #818cf8; font-weight: 700; font-size: 1.1rem; }

  body[data-theme="light"] .experiment-box { background: rgba(16, 185, 129, 0.05) !important; }
  body[data-theme="light"] .experiment-box h4 { color: #059669 !important; }
  body[data-theme="light"] .predict-challenge { background: rgba(245, 158, 11, 0.05) !important; }
  body[data-theme="light"] .predict-challenge h4 { color: #d97706 !important; }
  body[data-theme="light"] .predict-reveal summary { color: #d97706 !important; }
  body[data-theme="light"] .predict-answer { background: rgba(0, 0, 0, 0.05) !important; }
  body[data-theme="light"] .flow-diagram { background: rgba(99, 102, 241, 0.05) !important; }
  body[data-theme="light"] .flow-diagram h4 { color: #4f46e5 !important; }
  body[data-theme="light"] .flow-step { background: rgba(0,0,0,0.03) !important; }
`}</style>
