---
layout: ../../../layouts/CourseLayout.astro
title: "Section 2.3 • Compilation Pipeline"
description: "Understanding how C# source code becomes a running program"
---

import Checkpoint from '../../../components/Checkpoint.astro';
import DeepDiveTabs from '../../../components/DeepDiveTabs.astro';
import TryItNowRunner from '../../../components/TryItNowRunner.astro';

# Section 2.3: The Compilation Pipeline

<div class="section-header">
  <div class="section-badge">⚙️ Build Process</div>
  <p class="section-time">⏱️ Estimated Time: 25 minutes</p>
</div>

---

<div class="concept-intro">
  <div class="concept-text">
    <h2>✦ The C# Assembly Line: How Code Becomes Action</h2>
    <p>The C# compilation pipeline is a multi-stage transformation. Your human-readable source code doesn't talk to the CPU directly; instead, it travels through a specialized assembly line that turns <strong>syntax into symbols</strong>, and <strong>symbols into cross-platform instructions</strong> that can run on any device.</p>
  </div>
  <img src="/media/week%202/lesson%202.3/Assemblyline.png" alt="Assembly line factory representing the C# compilation process" class="concept-image" />
</div>

---

## 🔄 From Text to Running Program

When you write C# code, you're writing **human-readable text**. But your computer's CPU doesn't understand C#—it only understands machine code (binary instructions specific to your processor). The compilation pipeline bridges this gap.

<div class="callout-info">
  <span class="callout-icon">💡</span>
  <div>
    <strong>The Key Insight</strong>
    <p>C# uses a <strong>two-stage compilation</strong> process. This is what makes .NET applications portable across different operating systems.</p>
  </div>
</div>

---

## The Four Stages of the Pipeline

<div class="pipeline-carousel">
  <button class="carousel-arrow carousel-arrow-left" aria-label="Scroll backward">‹</button>
  <div class="pipeline-cards">
    <div class="pipeline-card">
      <h3>1. Raw Materials: Source Code</h3>
      <p>The journey begins with your <code>.cs</code> files. At this stage, the code is just text. The compiler performs <strong>Lexical Analysis</strong>, breaking your text into 'tokens' like keywords, names, and operators.</p>
    </div>
    <div class="pipeline-card">
      <h3>2. The Factory: Roslyn Compiler</h3>
      <p>Named after the 'Roslyn' platform, the C# compiler builds a complex digital map called an <strong>Abstract Syntax Tree (AST)</strong>. It checks for grammar errors and ensures your logic follows C# rules.</p>
    </div>
    <div class="pipeline-card">
      <h3>3. Intermediate: MSIL Bytecode</h3>
      <p>Instead of making machine code for one CPU, Roslyn outputs <strong>Microsoft Intermediate Language (MSIL)</strong>. This is the 'glowing' secret to C#'s cross-platform power—one set of instructions for all OSs.</p>
    </div>
    <div class="pipeline-card">
      <h3>4. Finished Product: Native Code</h3>
      <p>When you click 'Run,' the <strong>JIT (Just-In-Time) Compiler</strong> takes the MSIL and translates it into the specific language of your hardware (Windows, Mac, or Linux) right before it executes.</p>
    </div>
  </div>
  <button class="carousel-arrow carousel-arrow-right" aria-label="Scroll forward">›</button>
</div>

<script>
{`
  document.addEventListener('DOMContentLoaded', () => {
    const carousel = document.querySelector('.pipeline-carousel');
    if (!carousel) return;
    
    const cards = carousel.querySelector('.pipeline-cards');
    const leftBtn = carousel.querySelector('.carousel-arrow-left');
    const rightBtn = carousel.querySelector('.carousel-arrow-right');
    
    const scrollAmount = 340;
    
    leftBtn.addEventListener('click', () => {
      cards.scrollBy({ left: -scrollAmount, behavior: 'smooth' });
    });
    
    rightBtn.addEventListener('click', () => {
      cards.scrollBy({ left: scrollAmount, behavior: 'smooth' });
    });
  });
`}
</script>

---

## 📊 The Complete Pipeline

<div class="pipeline-diagram">
  <div class="pipeline-stage source">
    <div class="stage-icon">📝</div>
    <div class="stage-content">
      <h4>Source Code</h4>
      <p>Your <code>.cs</code> files</p>
      <code class="stage-example">Program.cs</code>
    </div>
  </div>
  
  <div class="pipeline-arrow">
    <span class="arrow-label">Roslyn Compiler</span>
    <span class="arrow-icon">→</span>
  </div>
  
  <div class="pipeline-stage msil">
    <div class="stage-icon">📦</div>
    <div class="stage-content">
      <h4>MSIL</h4>
      <p>Microsoft Intermediate Language</p>
      <code class="stage-example">.dll / .exe</code>
    </div>
  </div>
  
  <div class="pipeline-arrow">
    <span class="arrow-label">CLR (at runtime)</span>
    <span class="arrow-icon">→</span>
  </div>
  
  <div class="pipeline-stage native">
    <div class="stage-icon">⚡</div>
    <div class="stage-content">
      <h4>Machine Code</h4>
      <p>CPU-specific instructions</p>
      <code class="stage-example">Binary</code>
    </div>
  </div>
</div>

---

## The 'Compile Once, Run Anywhere' Architecture

This two-step process (Source → MSIL → Machine Code) is why a single .NET assembly can run on an Intel-based Windows PC, an ARM-based MacBook, or a Linux server without being recompiled.

<DeepDiveTabs
  title="Pipeline Secrets"
  tabs={[
    {
      id: "intermediate-language",
      label: "Why Intermediate Language?",
      content: "Think of MSIL as <strong>Esperanto</strong> for computers. By targeting an intermediate language, .NET allows different languages (C#, F#, VB.NET) to talk to each other.",
      keyPoints: [
        "<strong>Interoperability:</strong> You can use a library written in F# from your C# code because they both end up as the same MSIL.",
        "<strong>Hardware Agnostic:</strong> The developer doesn't need to know if the user is on a 64-bit or 32-bit system."
      ]
    },
    {
      id: "roslyn-service",
      label: "Roslyn: Compiler as a Service",
      content: "Old compilers were 'Black Boxes' (Source in, Binary out). <strong>Roslyn</strong> opened the box.",
      keyPoints: [
        "<strong>Analyzers:</strong> Visual Studio uses Roslyn to show you red squiggly lines while you type.",
        "<strong>Code Fixes:</strong> When you click the 'lightbulb,' Roslyn is literally rewriting your code's syntax tree for you."
      ]
    },
    {
      id: "jit-speed",
      label: "JIT: The Speed Secret",
      content: "Because the JIT compiler runs at the last second, it knows <strong>exactly</strong> what hardware it's on.",
      keyPoints: [
        "<strong>Hardware Specific:</strong> It can use the latest instructions on a high-end CPU that weren't available when you wrote the code.",
        "<strong>PGO (Profile-Guided Optimization):</strong> Modern JITs watch which paths your code takes most often and re-optimize while the program is running."
      ]
    }
  ]}
/>

---

## 🔬 Stage 1: Roslyn Compiler

**Roslyn** is the official C# compiler developed by Microsoft. When you build your project, Roslyn:

1. **Parses** your source code into an Abstract Syntax Tree (AST)
2. **Analyzes** the code for errors (type mismatches, missing semicolons, etc.)
3. **Transforms** the C# into **MSIL** (Microsoft Intermediate Language)
4. **Packages** the MSIL into an assembly (`.dll` or `.exe`)

```bash
# When you run this command:
dotnet build

# Roslyn compiles your .cs files into MSIL
# Output: bin/Debug/net8.0/YourApp.dll
```

<div class="tech-detail">
  <h4>🔍 What is Roslyn?</h4>
  <p>Roslyn is more than a compiler—it's a <strong>compiler-as-a-service</strong>. It powers:</p>
  <ul>
    <li>IntelliSense (code completion)</li>
    <li>Real-time error detection (red squiggles)</li>
    <li>Code refactoring tools</li>
    <li>Code analyzers and fixers</li>
  </ul>
</div>

---

## 🎯 Try It: Compile-Time vs Runtime Errors

When Roslyn compiles your code, it catches **syntax errors** immediately. But some errors only appear when the program runs:

<TryItNowRunner
  starterId="week-02-compile-errors"
  title="Spot the Error Type"
  code={`using System;

namespace ErrorTypes
{
    class Program
    {
        static void Main()
        {
            // This is a COMPILE-TIME error
            // The compiler catches it before the program runs
            // Console.WriteLin("Missing 'e' in WriteLine");
            
            // This compiles fine but causes a RUNTIME error
            int[] numbers = { 1, 2, 3 };
            Console.WriteLine("Array has 3 items");
            
            // Uncomment this to see a runtime error:
            // Console.WriteLine(numbers[10]); // Index out of bounds!
            
            Console.WriteLine("Program completed successfully!");
        }
    }
}`}
  height={400}
/>

<div class="experiment-box">
  <h4>🧪 Experiment</h4>
  <ol>
    <li>Run the code—it works!</li>
    <li>Uncomment line 11 (WriteLin) — <strong>compile error</strong> appears instantly</li>
    <li>Re-comment line 11, then uncomment line 18 (numbers[10])</li>
    <li>Run again—the code <strong>compiles</strong> but crashes at <strong>runtime</strong></li>
  </ol>
</div>

---

## 📦 Stage 2: MSIL (The Secret Sauce)

**MSIL** (Microsoft Intermediate Language), also called **IL** or **CIL** (Common Intermediate Language), is a CPU-independent instruction set.

<div class="architect-note">
  <div class="architect-avatar">👷‍♂️</div>
  <div class="architect-content">
    <h4>Why MSIL Makes C# Cross-Platform</h4>
    <p>
      Here's the magic: when you compile C# on Windows, you get MSIL. That same MSIL file can run on:
    </p>
    <ul>
      <li>🪟 Windows (x64, ARM)</li>
      <li>🐧 Linux (x64, ARM)</li>
      <li>🍎 macOS (x64, Apple Silicon)</li>
    </ul>
    <p>
      <strong>Compile once, run anywhere.</strong> The local machine's CLR handles the final translation to native code.
    </p>
  </div>
</div>

### What Does MSIL Look Like?

Your C# code:
```csharp
Console.WriteLine("Hello, World!");
```

Becomes MSIL:
```
IL_0000: ldstr "Hello, World!"
IL_0005: call void [System.Console]System.Console::WriteLine(string)
IL_000a: ret
```

You don't write MSIL directly—the compiler generates it. But understanding it exists helps you grasp how .NET achieves portability.

---

## ⚡ Stage 3: CLR (Common Language Runtime)

The **CLR** is the execution engine that runs your MSIL at runtime. It performs:

<div class="clr-features">
  <div class="clr-feature">
    <span class="feature-icon">🔄</span>
    <div>
      <strong>JIT Compilation</strong>
      <p>Just-In-Time: Converts MSIL to native machine code as needed</p>
    </div>
  </div>
  
  <div class="clr-feature">
    <span class="feature-icon">🧹</span>
    <div>
      <strong>Garbage Collection</strong>
      <p>Automatically frees memory when objects are no longer used</p>
    </div>
  </div>
  
  <div class="clr-feature">
    <span class="feature-icon">🔒</span>
    <div>
      <strong>Type Safety</strong>
      <p>Ensures type rules are enforced at runtime</p>
    </div>
  </div>
  
  <div class="clr-feature">
    <span class="feature-icon">🛡️</span>
    <div>
      <strong>Exception Handling</strong>
      <p>Manages errors and stack unwinding</p>
    </div>
  </div>
</div>

### JIT Compilation Explained

When your program runs:

1. CLR loads the MSIL assembly
2. When a method is **first called**, the JIT compiler translates it to native code
3. The native code is **cached** in memory
4. Subsequent calls use the cached native code (fast!)

```
First call to PrintMessage():
  MSIL → JIT Compile → Native Code → Execute → Cache

Second call to PrintMessage():
  Use Cached Native Code → Execute (no recompilation!)
```

---

## 🌍 Why This Architecture Matters

<div class="comparison-table">
  <div class="comparison-row header">
    <div class="comp-cell">Approach</div>
    <div class="comp-cell">Compile To</div>
    <div class="comp-cell">Portability</div>
  </div>
  <div class="comparison-row">
    <div class="comp-cell"><strong>C/C++</strong></div>
    <div class="comp-cell">Native machine code directly</div>
    <div class="comp-cell">❌ Must recompile for each OS/CPU</div>
  </div>
  <div class="comparison-row">
    <div class="comp-cell"><strong>C# (.NET)</strong></div>
    <div class="comp-cell">MSIL (intermediate)</div>
    <div class="comp-cell">✅ Same binary runs everywhere</div>
  </div>
  <div class="comparison-row">
    <div class="comp-cell"><strong>Java</strong></div>
    <div class="comp-cell">Bytecode (similar concept)</div>
    <div class="comp-cell">✅ JVM handles translation</div>
  </div>
</div>

---

## 🎯 The Build Process in Action

Here's what happens when you run `dotnet run`:

<div class="build-steps">
  <div class="build-step">
    <span class="step-number">1</span>
    <div class="step-content">
      <strong>dotnet run</strong>
      <p>You execute the command</p>
    </div>
  </div>
  
  <div class="build-step">
    <span class="step-number">2</span>
    <div class="step-content">
      <strong>Restore Dependencies</strong>
      <p>NuGet packages are downloaded if needed</p>
    </div>
  </div>
  
  <div class="build-step">
    <span class="step-number">3</span>
    <div class="step-content">
      <strong>Roslyn Compiles</strong>
      <p>.cs files → MSIL assembly (.dll)</p>
    </div>
  </div>
  
  <div class="build-step">
    <span class="step-number">4</span>
    <div class="step-content">
      <strong>CLR Loads Assembly</strong>
      <p>Runtime initializes and loads your code</p>
    </div>
  </div>
  
  <div class="build-step">
    <span class="step-number">5</span>
    <div class="step-content">
      <strong>Find Main()</strong>
      <p>Runtime locates the entry point</p>
    </div>
  </div>
  
  <div class="build-step">
    <span class="step-number">6</span>
    <div class="step-content">
      <strong>JIT & Execute</strong>
      <p>Methods are JIT-compiled and run</p>
    </div>
  </div>
</div>

---

## 🛠️ Seeing the Output

When you build a .NET project, check the output folder:

```
📁 bin/
  📁 Debug/
    📁 net8.0/
      📄 YourApp.dll      ← Your MSIL assembly
      📄 YourApp.exe      ← Windows launcher (calls the DLL)
      📄 YourApp.deps.json ← Dependency manifest
      📄 YourApp.runtimeconfig.json ← Runtime settings
```

The `.dll` file contains your compiled MSIL. It's portable across all platforms with a compatible .NET runtime.

---

## 🎯 Try It: Watch the Pipeline

Every time you click 'Run,' the entire compilation pipeline executes. This demo shows how different stages report issues:

<TryItNowRunner
  starterId="week-02-pipeline-demo"
  title="The Full Journey: Source → MSIL → Native"
  code={`using System;

namespace PipelineDemo
{
    class Program
    {
        static void Main()
        {
            // Stage 1: ROSLYN reads this source code
            // It checks syntax, types, and semantics
            
            string stage1 = "1. Your source code (.cs)";
            string stage2 = "2. Roslyn compiles to MSIL";
            string stage3 = "3. CLR loads the assembly";
            string stage4 = "4. JIT compiles to native code";
            
            Console.WriteLine("=== C# Compilation Pipeline ===");
            Console.WriteLine();
            Console.WriteLine(stage1);
            Console.WriteLine("   ↓");
            Console.WriteLine(stage2);
            Console.WriteLine("   ↓");
            Console.WriteLine(stage3);
            Console.WriteLine("   ↓");
            Console.WriteLine(stage4);
            Console.WriteLine("   ↓");
            Console.WriteLine("5. YOUR CODE RUNS! ✓");
            
            // Try adding a syntax error to see Roslyn catch it!
        }
    }
}`}
  height={450}
/>

<div class="experiment-box">
  <h4>🧪 Experiment</h4>
  <ol>
    <li>Run the code—watch the pipeline stages print</li>
    <li>Delete a semicolon—Roslyn (Stage 2) catches it immediately</li>
    <li>Fix it, then add <code>int x = "text";</code>—type error caught at compile time</li>
    <li>The program never reaches runtime if Roslyn finds issues!</li>
  </ol>
</div>

---

## 📋 Key Takeaways

<div class="takeaways">
  <div class="takeaway">
    <span class="takeaway-icon">📝</span>
    <div>
      <strong>Roslyn compiles C# to MSIL</strong>
      <p>The first stage transforms human-readable code into intermediate language.</p>
    </div>
  </div>
  
  <div class="takeaway">
    <span class="takeaway-icon">🌍</span>
    <div>
      <strong>MSIL enables cross-platform</strong>
      <p>The same compiled binary runs on Windows, Linux, and macOS.</p>
    </div>
  </div>
  
  <div class="takeaway">
    <span class="takeaway-icon">⚡</span>
    <div>
      <strong>CLR JIT-compiles at runtime</strong>
      <p>The Common Language Runtime translates MSIL to native code when methods are first called.</p>
    </div>
  </div>
</div>

---

## 🧠 Check Your Understanding

<Checkpoint
  checkpointId="week-02-2-3-compilation"
  title="Compilation Pipeline"
  questions={[
    {
      id: "q1",
      type: "multiple-choice",
      question: "What intermediate format does the Roslyn compiler produce?",
      options: [
        "Binary machine code",
        "JavaScript",
        "MSIL (Microsoft Intermediate Language)",
        "Assembly language"
      ],
      correctAnswer: "MSIL (Microsoft Intermediate Language)",
      explanation: "Roslyn compiles C# into MSIL, a platform-independent format. The JIT compiler then converts MSIL to native machine code at runtime."
    },
    {
      id: "q2",
      type: "multiple-choice",
      question: "When does the JIT (Just-In-Time) compiler run?",
      options: [
        "When you save your .cs file",
        "When you run 'dotnet build'",
        "At runtime, just before code executes",
        "During installation of .NET SDK"
      ],
      correctAnswer: "At runtime, just before code executes",
      explanation: "The JIT compiler runs at runtime, converting MSIL to native machine code right before it executes. This allows .NET to optimize for the specific hardware it's running on."
    },
    {
      id: "q3",
      type: "multiple-choice",
      question: "Why is the two-stage compilation (Source → MSIL → Machine Code) beneficial?",
      options: [
        "It makes programs run faster",
        "It reduces file size",
        "It enables cross-platform portability",
        "It eliminates all bugs"
      ],
      correctAnswer: "It enables cross-platform portability",
      explanation: "The same MSIL can run on Windows, Mac, or Linux because the JIT compiler on each platform translates it to the appropriate native code. This is the 'compile once, run anywhere' advantage."
    }
  ]}
/>

---

<div class="next-section">
  <p>Next up: <strong>Section 2.4 - Code Style & Conventions</strong></p>
  <p>Learn professional formatting standards that make your code readable and maintainable.</p>
  <a href="/week-02/2-4-style/" class="vscode-button">Continue to Section 2.4 →</a>
</div>

<style is:global>
{`
  /* Concept Intro with Image */
  .concept-intro {
    display: flex;
    gap: 32px;
    align-items: center;
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
    border-radius: 16px;
    padding: 32px;
    margin: 32px 0;
  }
  
  .concept-text {
    flex: 1;
  }
  
  .concept-text h2 {
    color: #4ec9b0;
    margin-top: 0;
    font-size: 1.5rem;
  }
  
  .concept-text p {
    color: #c0c0c0;
    line-height: 1.7;
    font-size: 1.05rem;
    margin-bottom: 0;
  }
  
  .concept-image {
    width: 280px;
    height: auto;
    border-radius: 12px;
    flex-shrink: 0;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
  }
  
  @media (max-width: 768px) {
    .concept-intro {
      flex-direction: column;
    }
    
    .concept-image {
      width: 100%;
      max-width: 320px;
    }
  }

  /* Pipeline Carousel */
  .pipeline-carousel {
    display: flex;
    align-items: center;
    gap: 12px;
    margin: 32px 0;
    position: relative;
  }
  
  .pipeline-cards {
    display: flex;
    gap: 16px;
    overflow-x: auto;
    scroll-behavior: smooth;
    scrollbar-width: none;
    -ms-overflow-style: none;
    padding: 8px 4px;
    flex: 1;
  }
  
  .pipeline-cards::-webkit-scrollbar {
    display: none;
  }
  
  .pipeline-card {
    background: #2a2a3a;
    border-radius: 12px;
    padding: 24px;
    min-width: 280px;
    max-width: 300px;
    flex-shrink: 0;
  }
  
  .pipeline-card h3 {
    color: #fff;
    font-size: 1.1rem;
    margin: 0 0 12px 0;
    line-height: 1.3;
  }
  
  .pipeline-card p {
    color: #b0b0b0;
    font-size: 0.95rem;
    line-height: 1.6;
    margin: 0;
  }
  
  .pipeline-card code {
    background: #1a1a2e;
    padding: 2px 6px;
    border-radius: 4px;
    font-size: 0.9rem;
    color: #4ec9b0;
  }
  
  .pipeline-card strong {
    color: #fff;
  }
  
  .carousel-arrow {
    background: #3a3a4a;
    border: none;
    color: #fff;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    font-size: 1.5rem;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    transition: background 0.2s, transform 0.2s;
  }
  
  .carousel-arrow:hover {
    background: #4a4a5a;
    transform: scale(1.1);
  }

  .section-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 12px;
    margin-bottom: 24px;
  }
  
  .section-badge {
    background: linear-gradient(135deg, #4ec9b0 0%, #3ba892 100%);
    color: #000;
    padding: 8px 16px;
    border-radius: 20px;
    font-weight: 600;
    font-size: 0.9rem;
  }
  
  .section-time {
    color: #888;
    margin: 0;
  }
  
  .callout-info {
    display: flex;
    gap: 16px;
    background: linear-gradient(135deg, #1a2a3a 0%, #1a1a2e 100%);
    border-left: 4px solid #4ec9b0;
    border-radius: 8px;
    padding: 20px;
    margin: 24px 0;
  }
  
  .callout-icon {
    font-size: 2rem;
    flex-shrink: 0;
  }
  
  .callout-info strong {
    color: #4ec9b0;
  }
  
  .callout-info p {
    margin: 8px 0 0;
  }
  
  .pipeline-diagram {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 16px;
    padding: 32px;
    background: #0d1117;
    border-radius: 16px;
    margin: 24px 0;
  }
  
  .pipeline-stage {
    display: flex;
    flex-direction: column;
    align-items: center;
    padding: 20px;
    background: #1e1e2e;
    border-radius: 12px;
    min-width: 140px;
    text-align: center;
  }
  
  .pipeline-stage.source {
    border: 2px solid #61afef;
  }
  
  .pipeline-stage.msil {
    border: 2px solid #c678dd;
  }
  
  .pipeline-stage.native {
    border: 2px solid #98c379;
  }
  
  .stage-icon {
    font-size: 2.5rem;
    margin-bottom: 12px;
  }
  
  .stage-content h4 {
    margin: 0;
    color: #e0e0e0;
    font-size: 1rem;
  }
  
  .stage-content p {
    margin: 4px 0;
    color: #888;
    font-size: 0.8rem;
  }
  
  .stage-example {
    display: inline-block;
    margin-top: 8px;
    padding: 4px 8px;
    background: #0d1117;
    border-radius: 4px;
    font-size: 0.8rem;
    color: #4ec9b0;
  }
  
  .pipeline-arrow {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
  }
  
  .arrow-label {
    font-size: 0.75rem;
    color: #888;
    white-space: nowrap;
  }
  
  .arrow-icon {
    font-size: 1.5rem;
    color: #4ec9b0;
  }
  
  .tech-detail {
    background: #1e1e2e;
    border-radius: 12px;
    padding: 20px;
    margin: 20px 0;
  }
  
  .tech-detail h4 {
    margin-top: 0;
    color: #61afef;
  }
  
  .tech-detail ul {
    margin-bottom: 0;
  }
  
  .architect-note {
    display: flex;
    gap: 20px;
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
    border-left: 4px solid #f0ad4e;
    border-radius: 8px;
    padding: 24px;
    margin: 24px 0;
  }
  
  .architect-avatar {
    font-size: 3rem;
    flex-shrink: 0;
  }
  
  .architect-content h4 {
    margin-top: 0;
    color: #f0ad4e;
  }
  
  .architect-content ul {
    margin: 12px 0;
  }
  
  .clr-features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 16px;
    margin: 24px 0;
  }
  
  .clr-feature {
    display: flex;
    gap: 16px;
    background: #1e1e2e;
    padding: 16px;
    border-radius: 8px;
  }
  
  .feature-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
  }
  
  .clr-feature strong {
    color: #4ec9b0;
    display: block;
  }
  
  .clr-feature p {
    margin: 4px 0 0;
    color: #888;
    font-size: 0.9rem;
  }
  
  .comparison-table {
    border-radius: 12px;
    overflow: hidden;
    margin: 24px 0;
  }
  
  .comparison-row {
    display: grid;
    grid-template-columns: 1fr 1.5fr 1.5fr;
    gap: 1px;
    background: #30363d;
  }
  
  .comparison-row.header {
    background: #4ec9b0;
  }
  
  .comparison-row.header .comp-cell {
    background: #4ec9b0;
    color: #000;
    font-weight: 600;
  }
  
  .comp-cell {
    padding: 12px 16px;
    background: #1e1e2e;
  }
  
  .build-steps {
    display: flex;
    flex-direction: column;
    gap: 12px;
    margin: 24px 0;
  }
  
  .build-step {
    display: flex;
    align-items: center;
    gap: 16px;
    background: #1e1e2e;
    padding: 16px 20px;
    border-radius: 8px;
  }
  
  .step-number {
    background: linear-gradient(135deg, #4ec9b0 0%, #3ba892 100%);
    color: #000;
    width: 32px;
    height: 32px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    flex-shrink: 0;
  }
  
  .step-content strong {
    color: #e0e0e0;
    display: block;
  }
  
  .step-content p {
    margin: 4px 0 0;
    color: #888;
    font-size: 0.9rem;
  }
  
  .takeaways {
    display: flex;
    flex-direction: column;
    gap: 16px;
    margin: 24px 0;
  }
  
  .takeaway {
    display: flex;
    gap: 16px;
    background: #1e1e2e;
    padding: 16px 20px;
    border-radius: 8px;
    border-left: 3px solid #4ec9b0;
  }
  
  .takeaway-icon {
    font-size: 1.5rem;
    flex-shrink: 0;
  }
  
  .takeaway strong {
    color: #4ec9b0;
  }
  
  .takeaway p {
    margin: 4px 0 0;
    color: #a0a0a0;
    font-size: 0.9rem;
  }
  
  .next-section {
    background: linear-gradient(135deg, #1e2e1e 0%, #1a2a1a 100%);
    border: 1px solid #4ec9b0;
    border-radius: 12px;
    padding: 24px;
    text-align: center;
    margin-top: 40px;
  }
  
  .next-section p {
    margin: 8px 0;
  }
  
  .next-section a {
    margin-top: 16px;
  }

  /* Light mode overrides */
  body[data-theme="light"] .concept-intro {
    background: linear-gradient(135deg, #f0f8ff 0%, #e8f4f8 100%) !important;
  }
  
  body[data-theme="light"] .concept-text h2 {
    color: #0d7377 !important;
  }
  
  body[data-theme="light"] .concept-text p {
    color: #333 !important;
  }

  body[data-theme="light"] .pipeline-card {
    background: #f5f5f5 !important;
    border: 1px solid #ddd;
  }

  body[data-theme="light"] .pipeline-card h3 {
    color: #333 !important;
  }

  body[data-theme="light"] .pipeline-card p {
    color: #555 !important;
  }

  body[data-theme="light"] .pipeline-card strong {
    color: #333 !important;
  }

  body[data-theme="light"] .pipeline-card code {
    background: #e8e8e8 !important;
    color: #0d7377 !important;
  }

  body[data-theme="light"] .carousel-arrow {
    background: #e0e0e0 !important;
    color: #333 !important;
  }

  body[data-theme="light"] .carousel-arrow:hover {
    background: #d0d0d0 !important;
  }

  body[data-theme="light"] .architect-note {
    background: linear-gradient(135deg, #f8f4e8 0%, #f0ebe0 100%) !important;
  }

  body[data-theme="light"] .architect-content p {
    color: #333 !important;
  }

  body[data-theme="light"] .architect-content p strong {
    color: #b8860b !important;
  }

  body[data-theme="light"] .comp-cell {
    background: #f5f5f5 !important;
    color: #333 !important;
  }

  body[data-theme="light"] .build-step {
    background: #f5f5f5 !important;
    border: 1px solid #ddd;
  }

  body[data-theme="light"] .build-step span {
    color: #333 !important;
  }

  body[data-theme="light"] .takeaway {
    background: #f0f8f0 !important;
  }

  body[data-theme="light"] .takeaway p {
    color: #555 !important;
  }

  body[data-theme="light"] .next-section {
    background: linear-gradient(135deg, #e8f8e8 0%, #e0f0e0 100%) !important;
  }

  body[data-theme="light"] .next-section p {
    color: #333 !important;
  }

  body[data-theme="light"] .pipeline-container {
    background: #f5f5f5 !important;
    border: 1px solid #ddd;
  }

  body[data-theme="light"] .pipeline-stage {
    background: #fff !important;
    border: 1px solid #ddd;
  }

  body[data-theme="light"] .stage-content h4 {
    color: #333 !important;
  }

  body[data-theme="light"] .stage-content code {
    color: #555 !important;
  }

  body[data-theme="light"] .comparison-row .comp-cell {
    background: #fff !important;
    color: #333 !important;
  }

  body[data-theme="light"] .jit-steps {
    background: #f5f5f5 !important;
    border: 1px solid #ddd;
  }

  /* Section header */
  body[data-theme="light"] .section-time {
    color: #666 !important;
  }

  /* Callout info */
  body[data-theme="light"] .callout-info {
    background: linear-gradient(135deg, #e8f4f8 0%, #f0f8ff 100%) !important;
    border-left-color: #0d7377;
  }

  body[data-theme="light"] .callout-info strong {
    color: #0d7377 !important;
  }

  body[data-theme="light"] .callout-info p {
    color: #333 !important;
  }

  /* Pipeline diagram */
  body[data-theme="light"] .pipeline-diagram {
    background: #f5f5f5 !important;
    border: 1px solid #ddd;
  }

  body[data-theme="light"] .pipeline-stage {
    background: #fff !important;
  }

  body[data-theme="light"] .pipeline-stage.source {
    border-color: #0066cc;
  }

  body[data-theme="light"] .pipeline-stage.msil {
    border-color: #8b4513;
  }

  body[data-theme="light"] .pipeline-stage.native {
    border-color: #2e8b57;
  }

  body[data-theme="light"] .stage-content h4 {
    color: #333 !important;
  }

  body[data-theme="light"] .stage-content p {
    color: #666 !important;
  }

  body[data-theme="light"] .stage-example {
    background: #e8e8e8 !important;
    color: #0d7377 !important;
  }

  body[data-theme="light"] .arrow-label {
    color: #666 !important;
  }

  body[data-theme="light"] .arrow-icon {
    color: #0d7377 !important;
  }

  /* Tech detail */
  body[data-theme="light"] .tech-detail {
    background: #f5f5f5 !important;
    border: 1px solid #ddd;
  }

  body[data-theme="light"] .tech-detail h4 {
    color: #0066cc !important;
  }

  /* CLR features */
  body[data-theme="light"] .clr-feature {
    background: #f5f5f5 !important;
    border: 1px solid #ddd;
  }

  body[data-theme="light"] .clr-feature strong {
    color: #0d7377 !important;
  }

  body[data-theme="light"] .clr-feature p {
    color: #555 !important;
  }

  /* Comparison table */
  body[data-theme="light"] .comparison-row {
    background: #ddd !important;
  }

  body[data-theme="light"] .comparison-row.header .comp-cell {
    background: #0d7377 !important;
    color: #fff !important;
  }

  body[data-theme="light"] .comp-cell {
    background: #fff !important;
    color: #333 !important;
  }

  /* Build steps */
  body[data-theme="light"] .build-step {
    background: #f5f5f5 !important;
    border: 1px solid #ddd;
  }

  body[data-theme="light"] .step-content strong {
    color: #333 !important;
  }

  body[data-theme="light"] .step-content p {
    color: #555 !important;
  }

  /* Architect note list items */
  body[data-theme="light"] .architect-content ul,
  body[data-theme="light"] .architect-content li {
    color: #333 !important;
  }
`}
</style>
