---
layout: ../../../layouts/CourseLayout.astro
title: "Section 3.4 • Immutability"
description: "Understanding constants and readonly values in C#"
---

import Checkpoint from '../../../components/Checkpoint.astro';
import TryItNowRunner from '../../../components/TryItNowRunner.astro';
import DeepDiveTabs from '../../../components/DeepDiveTabs.astro';

# Section 3.4: Immutability

<div class="section-header">
  <div class="section-badge">🔒 Constants & Readonly</div>
  <p class="section-time">⏱️ Estimated Time: 20 minutes</p>
</div>

---

## 🎬 Technical Briefing: Variables & Constants

<div class="video-section">
  <p class="video-intro">A concise guide on protecting system data using constants and avoiding common pitfalls during initialization.</p>
  <div class="video-container">
    <iframe 
      src="https://www.youtube.com/embed/HLn_kBaZMBs" 
      title="Variables & Constants in C# - Fundamentals"
      frameborder="0" 
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" 
      allowfullscreen>
    </iframe>
  </div>
</div>

---

<div class="concept-header">
  <div class="header-content">
    <div class="header-text">
      <h2>✦ Written in Stone: The 'const' Keyword</h2>
      <p>
        In C#, a <strong>constant</strong> is a value that is fixed forever the moment you compile your code. Unlike a regular variable which is a temporary parking spot, a constant is an <strong>engraved instruction</strong> baked directly into the machinery of your program. Once written, even the strongest digital 'laser' (your code) cannot overwrite it.
      </p>
    </div>
  </div>
</div>

---

<DeepDiveTabs
  title="Architecture of a Constant"
  tabs={[
    {
      id: "baking",
      label: "The 'Baking' Process",
      content: `
<h3>Compile-Time Inlining</h3>
<p>When you mark a value as <code>const</code>, the Roslyn compiler performs a <strong>'Find and Replace'</strong> across your entire project. It doesn't look up the value at runtime; it literally replaces every mention of your constant with the raw number or text.</p>
<ul>
  <li><strong>Performance:</strong> There is zero overhead. Your CPU doesn't 'fetch' a variable; the value is already there in the instruction.</li>
  <li><strong>The Versioning Trap:</strong> If you change a <code>const</code> in Library A but don't recompile Library B that uses it, Library B will still be using the old, 'baked-in' value.</li>
</ul>
      `
    },
    {
      id: "naming",
      label: "Naming Rules",
      content: `
<h3>Engineering Conventions</h3>
<p>Professional C# architects follow strict naming patterns to signal that a value is immutable. This allows teammates to know at a glance that they can't change the value.</p>
<ul>
  <li><strong>PascalCase:</strong> Standard .NET convention (e.g., <code>MaxConnectionsLimit</code>).</li>
  <li><strong>SCREAMING_SNAKE:</strong> Common in older C++ influenced projects (e.g., <code>MAX_RETRY_COUNT</code>).</li>
  <li><strong>Descriptive Nouns:</strong> Never use generic names. <code>SECONDS_PER_MINUTE</code> is architecture; <code>SIXTY</code> is just a number.</li>
</ul>
      `
    },
    {
      id: "limits",
      label: "Type Limits",
      content: `
<h3>What Can Be 'Stoned'?</h3>
<p>Because the compiler must know the value <em>before</em> the program starts, you can only use <strong>built-in primitive types</strong> as constants.</p>
<ul>
  <li><strong>Allowed:</strong> <code>int</code>, <code>double</code>, <code>bool</code>, <code>char</code>, <code>string</code>, and <code>decimal</code>.</li>
  <li><strong>Forbidden:</strong> Arrays, custom Classes, or anything that requires a <code>new</code> keyword (except for <code>null</code>). These must use the <code>readonly</code> keyword instead.</li>
</ul>
      `
    }
  ]}
/>

---

## 🔐 Why Make Values Unchangeable?

Some values should **never change** during program execution:

- Mathematical constants (π = 3.14159...)
- Configuration settings (MAX_CONNECTIONS = 100)
- Fixed rates (TAX_RATE = 0.0725)

Making these values **immutable** (unchangeable) prevents bugs and makes your intent clear.

---

## 🎯 The const Keyword

Use `const` to create a compile-time constant—a value that is **baked into your program** when it's built.

```csharp
const double PI = 3.14159265359;
const int MAX_ATTEMPTS = 3;
const string APP_NAME = "SystemMonitor";
```

### Rules for const:
1. Must be assigned at declaration
2. Value must be known at compile time
3. Cannot be changed after declaration

<TryItNowRunner 
  starterId="week-03-const-demo" 
  height={340}
  code={`using System;

class Program
{
    static void Main()
    {
        const double PI = 3.14159;
        const int MAX_USERS = 100;
        
        double radius = 5.0;
        double area = PI * radius * radius;
        
        Console.WriteLine("Circle area: " + area);
        Console.WriteLine("Max users allowed: " + MAX_USERS);
        
        // This would cause an error:
        // PI = 3.14;  // ❌ Cannot assign to const
    }
}`}
  instructions="Run this code. Try uncommenting the last line to see the compiler error!" 
/>

---

## ⚠️ What Happens If You Try to Change a const?

<div class="error-example">
  <h4>❌ Compiler Error</h4>
  <pre><code>const int MAX = 100;
MAX = 200;  // Error CS0131: The left-hand side of an assignment must be a variable</code></pre>
  <p>The compiler catches this mistake <strong>before</strong> your program even runs!</p>
</div>

---

## 📝 Naming Convention: UPPER_CASE

By convention, constants use `UPPER_CASE` with underscores:

```csharp
const int MAX_CONNECTIONS = 100;     // ✅ Clear it's a constant
const double TAX_RATE = 0.0725;      // ✅ Clear it's a constant
const string API_KEY = "abc123";     // ✅ Clear it's a constant

const int maxConnections = 100;      // ⚠️ Works, but not conventional
```

This naming pattern immediately signals to developers: "This value never changes."

---

## 🔄 const vs Regular Variables

<DeepDiveTabs
  title="When to Use const"
  tabs={[
    {
      id: "const",
      label: "🔒 Use const When...",
      content: `
        <h3>const is Perfect For:</h3>
        <ul>
          <li><strong>Mathematical constants:</strong> PI, E, gravity</li>
          <li><strong>Application settings:</strong> MAX_RETRIES, TIMEOUT_SECONDS</li>
          <li><strong>Fixed values:</strong> Days in a week, months in a year</li>
          <li><strong>Configuration:</strong> API endpoints, version numbers</li>
        </ul>
        <pre><code>const int DAYS_IN_WEEK = 7;
const double GRAVITY = 9.81;
const string VERSION = "1.0.0";</code></pre>
      `
    },
    {
      id: "variable",
      label: "🔄 Use Variables When...",
      content: `
        <h3>Variables are Better For:</h3>
        <ul>
          <li><strong>User input:</strong> Values entered at runtime</li>
          <li><strong>Calculated results:</strong> Values computed during execution</li>
          <li><strong>State tracking:</strong> Score, count, position</li>
          <li><strong>Anything that changes:</strong> If it might change, don't use const</li>
        </ul>
        <pre><code>int currentScore = 0;        // Changes during game
string userName = "Guest";   // Changes after login
double balance = 100.00;     // Changes with transactions</code></pre>
      `
    }
  ]}
/>

---

## 🎯 Practical Example: Configuration Constants

<TryItNowRunner 
  starterId="week-03-config-constants" 
  height={400}
  code={`using System;

class Program
{
    // Configuration constants at the top
    const string APP_NAME = "SystemMonitor";
    const string VERSION = "2.1.0";
    const int MAX_CONNECTIONS = 100;
    const int TIMEOUT_SECONDS = 30;
    
    static void Main()
    {
        Console.WriteLine("=========================");
        Console.WriteLine($"  {APP_NAME} v{VERSION}");
        Console.WriteLine("=========================");
        Console.WriteLine($"Max Connections: {MAX_CONNECTIONS}");
        Console.WriteLine($"Timeout: {TIMEOUT_SECONDS}s");
        Console.WriteLine("System initialized.");
    }
}`}
  instructions="This shows a common pattern: configuration constants at the top of your program." 
/>

---

## 💡 The Senior Architect's Perspective

<div class="architect-note">
  <div class="architect-avatar">👷‍♂️</div>
  <div class="architect-content">
    <h4>From the Field: Magic Numbers</h4>
    <p>
      A "magic number" is a hardcoded value with no explanation:
    </p>
    <pre><code>if (attempts > 3)  // What is 3? Why 3?</code></pre>
    <p>
      Replace magic numbers with named constants:
    </p>
    <pre><code>const int MAX_ATTEMPTS = 3;
if (attempts > MAX_ATTEMPTS)  // Now it's clear!</code></pre>
    <p>
      <strong>Rule:</strong> If a number appears in your code and isn't obviously self-explanatory (like 0 or 1), give it a name.
    </p>
  </div>
</div>

---

## 📊 Quick Reference

<div class="styled-table-container">
  <table>
    <thead>
      <tr>
        <th>Feature</th>
        <th><code>const</code></th>
        <th>Regular Variable</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Can be changed</td>
        <td>❌ No</td>
        <td>✅ Yes</td>
      </tr>
      <tr>
        <td>When value is set</td>
        <td>Compile time</td>
        <td>Runtime</td>
      </tr>
      <tr>
        <td>Naming convention</td>
        <td>UPPER_CASE</td>
        <td>camelCase</td>
      </tr>
      <tr>
        <td>Use for</td>
        <td>Fixed values, config</td>
        <td>Changing data</td>
      </tr>
    </tbody>
  </table>
</div>

---

## 🧠 Knowledge Check

<Checkpoint
  checkpointId="week-03-3-4-checkpoint"
  title="🔍 Section 3.4 Knowledge Check (Participation)"
  questions={[
    {
      id: "cp1",
      type: "multiple-choice",
      question: "Which keyword makes a variable unchangeable after it is set?",
      options: ["static", "const", "final", "fixed"],
      correctAnswer: "const",
      explanation: "The const keyword creates a compile-time constant that cannot be modified."
    },
    {
      id: "cp2",
      type: "multiple-choice",
      question: "What is the conventional naming style for constants?",
      options: ["camelCase", "PascalCase", "UPPER_CASE", "lowercase"],
      correctAnswer: "UPPER_CASE",
      explanation: "Constants typically use UPPER_CASE with underscores to clearly identify them as unchangeable values."
    },
    {
      id: "cp3",
      type: "multiple-choice",
      question: "What happens if you try to assign a new value to a const?",
      options: [
        "The value changes normally",
        "Nothing happens (ignored)",
        "Compiler error before running",
        "Runtime error while running"
      ],
      correctAnswer: "Compiler error before running",
      explanation: "The compiler detects attempts to modify const values and prevents the program from compiling."
    }
  ]}
/>

---

<div class="next-section">
  <p>✅ <strong>Reading Complete!</strong></p>
  <p>You've learned all the data types for Week 03. Time to apply your knowledge!</p>
  <a href="/week-03/lab/" class="vscode-button">Start Lab 03: Data Manifest →</a>
</div>

<style>{`
  .error-example {
    background: rgba(239, 68, 68, 0.1);
    border: 1px solid #ef4444;
    border-radius: 8px;
    padding: 16px;
    margin: 20px 0;
  }

  .error-example h4 {
    margin-top: 0;
    color: #ef4444;
  }

  .architect-note {
    display: flex;
    gap: 20px;
    background: rgba(255, 193, 7, 0.1);
    border: 1px solid #ffc107;
    border-radius: 12px;
    padding: 24px;
    margin: 24px 0;
  }

  .architect-avatar {
    font-size: 48px;
    flex-shrink: 0;
  }

  .architect-content h4 {
    margin-top: 0;
    color: #ffc107;
  }

  .architect-content pre {
    background: rgba(0, 0, 0, 0.3);
    padding: 12px;
    border-radius: 6px;
    overflow-x: auto;
  }

  .next-section {
    background: linear-gradient(135deg, rgba(78, 201, 176, 0.15) 0%, rgba(107, 159, 255, 0.15) 100%);
    border: 2px solid #4ec9b0;
    border-radius: 12px;
    padding: 24px;
    text-align: center;
    margin: 32px 0;
  }
  
  .next-section p {
    margin: 8px 0;
    color: rgba(255, 255, 255, 0.9);
  }
  
  .next-section a {
    display: inline-block;
    margin-top: 16px;
  }

  @media (max-width: 768px) {
    .architect-note {
      flex-direction: column;
      text-align: center;
    }
  }

  /* Styled table */
  .styled-table-container {
    background: #1e2128;
    border-radius: 12px;
    padding: 8px 24px;
    margin: 24px 0;
    overflow-x: auto;
  }

  .styled-table-container table {
    width: 100%;
    border-collapse: collapse;
  }

  .styled-table-container th {
    padding: 16px;
    text-align: left;
    font-weight: 600;
    color: #fff;
    border-bottom: 2px solid rgba(78, 201, 176, 0.3);
  }

  .styled-table-container td {
    padding: 16px;
    text-align: left;
    color: rgba(255, 255, 255, 0.9);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .styled-table-container tr:last-child td {
    border-bottom: none;
  }

  .styled-table-container code {
    color: #4ec9b0;
    background: rgba(78, 201, 176, 0.15);
    padding: 2px 6px;
    border-radius: 4px;
  }

  body[data-theme="light"] .styled-table-container {
    background: #f8f9fa !important;
    border: 1px solid #e0e0e0;
  }

  body[data-theme="light"] .styled-table-container th {
    color: #333 !important;
    border-bottom-color: rgba(78, 201, 176, 0.5);
  }

  body[data-theme="light"] .styled-table-container td {
    color: #333 !important;
    border-bottom-color: rgba(0, 0, 0, 0.1);
  }

  body[data-theme="light"] .styled-table-container code {
    color: #0a7c62;
    background: rgba(78, 201, 176, 0.2);
  }

  /* Video Section */
  .video-section {
    background: linear-gradient(135deg, rgba(30, 33, 40, 0.8) 0%, rgba(20, 22, 28, 0.9) 100%);
    border: 1px solid rgba(99, 102, 241, 0.3);
    border-radius: 16px;
    padding: 24px;
    margin: 24px 0;
  }

  .video-intro {
    color: rgba(255, 255, 255, 0.85);
    margin-bottom: 20px;
    font-size: 1rem;
    line-height: 1.6;
  }

  .video-container {
    max-width: 100%;
    margin: 0 auto;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.4);
    aspect-ratio: 16 / 9;
  }

  .video-container iframe {
    display: block;
    width: 100%;
    height: 100%;
  }

  body[data-theme="light"] .video-section {
    background: linear-gradient(135deg, #f8f9fa 0%, #fff 100%);
    border-color: rgba(99, 102, 241, 0.2);
  }

  body[data-theme="light"] .video-intro {
    color: #333;
  }

  /* Concept Header */
  .concept-header {
    background: linear-gradient(135deg, rgba(30, 33, 40, 0.9) 0%, rgba(20, 22, 28, 0.95) 100%);
    border: 1px solid rgba(99, 102, 241, 0.3);
    border-radius: 16px;
    padding: 32px;
    margin: 32px 0;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
  }

  .header-content {
    display: flex;
    gap: 32px;
    align-items: center;
  }

  .header-text {
    flex: 1;
  }

  .header-text h2 {
    background: linear-gradient(90deg, #a78bfa 0%, #6b9fff 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-size: 1.8rem;
    font-weight: 700;
    margin: 0 0 16px 0;
  }

  .header-text p {
    color: rgba(255, 255, 255, 0.85);
    font-size: 1.05rem;
    line-height: 1.7;
    margin: 0;
  }

  .header-text code {
    background: rgba(78, 201, 176, 0.2);
    color: #4ec9b0;
    padding: 2px 8px;
    border-radius: 4px;
    font-family: 'JetBrains Mono', 'Fira Code', monospace;
  }

  .header-image {
    flex-shrink: 0;
    max-width: 280px;
  }

  .header-image img {
    width: 100%;
    height: auto;
    border-radius: 12px;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
  }

  body[data-theme="light"] .concept-header {
    background: linear-gradient(135deg, #f8f9fa 0%, #fff 100%);
    border-color: rgba(99, 102, 241, 0.2);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.08);
  }

  body[data-theme="light"] .header-text h2 {
    background: linear-gradient(90deg, #6366f1 0%, #3b82f6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  body[data-theme="light"] .header-text p {
    color: #333;
  }

  body[data-theme="light"] .header-text code {
    background: rgba(78, 201, 176, 0.15);
    color: #0a7c62;
  }

  @media (max-width: 768px) {
    .header-content {
      flex-direction: column;
    }

    .header-image {
      max-width: 100%;
    }
  }
`}</style>
