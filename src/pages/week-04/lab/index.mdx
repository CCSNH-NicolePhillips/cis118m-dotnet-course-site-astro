---
layout: ../../../layouts/CourseLayout.astro
title: "Lab 04 • Text Sanitizer"
description: "Clean and format messy user input using string methods"
---

import TryItNowRunner from '../../../components/TryItNowRunner.astro';
import DueDateBanner from '../../../components/DueDateBanner.astro';

# Lab 04: The Text Sanitizer

<DueDateBanner week={4} type="Lab" />

**Time: 45 minutes**  
**Points: 100**

## Mission Objective

You've received **messy user input** that needs to be cleaned up and formatted before it can be stored in the database. Use **string methods** to sanitize the data.

---

## The Problem

A user submitted their profile information, but it's full of problems:
- Extra spaces everywhere
- Inconsistent capitalization  
- Unwanted characters in the phone number
- Needs reformatting for display

**Your job:** Clean it up using the string methods you learned this week!

---

## Requirements

Your program must use these string methods to clean the data:

<div class="styled-table-container">
  <table>
    <thead>
      <tr>
        <th>Method</th>
        <th>What to Fix</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><code>Trim()</code></td>
        <td>Remove extra spaces from beginning/end of strings</td>
      </tr>
      <tr>
        <td><code>ToUpper()</code></td>
        <td>Convert the state code to uppercase</td>
      </tr>
      <tr>
        <td><code>Replace()</code></td>
        <td>Remove parentheses and spaces from phone number</td>
      </tr>
      <tr>
        <td><code>Substring()</code></td>
        <td>Extract the area code (first 3 digits) from phone</td>
      </tr>
      <tr>
        <td><code>Length</code></td>
        <td>Display the character count of the bio</td>
      </tr>
      <tr>
        <td>String Interpolation</td>
        <td>Format the final output using <code>$"..."</code></td>
      </tr>
    </tbody>
  </table>
</div>

---

## Required Output

Your cleaned output should look **exactly** like this:

```
=== SANITIZED USER PROFILE ===
Name: John Smith
Email: john.smith@email.com
Phone: 555-123-4567
Area Code: 555
City: Boston
State: MA
Bio: Loves coding and coffee
Bio Length: 27 characters
==============================
Profile sanitization complete!
```

---

## Code Editor

<div id="lab-editor" style="border: 1px solid var(--border, #444); border-radius: 8px; overflow: hidden; margin: 1rem 0;">
  <iframe 
    src="/editor/embedded?starter=week-04-lab" 
    style="width: 100%; height: 600px; border: none;"
    title="Lab 4 Code Editor"
  ></iframe>
</div>

---

## Grading Rubric

<div class="styled-table-container">
  <table>
    <thead>
      <tr>
        <th>Criteria</th>
        <th>Points</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>Uses <code>Trim()</code> to remove extra whitespace</td>
        <td>15</td>
      </tr>
      <tr>
        <td>Uses <code>ToUpper()</code> for state code</td>
        <td>15</td>
      </tr>
      <tr>
        <td>Uses <code>Replace()</code> to clean phone number</td>
        <td>20</td>
      </tr>
      <tr>
        <td>Uses <code>Substring()</code> to extract area code</td>
        <td>20</td>
      </tr>
      <tr>
        <td>Uses <code>Length</code> property correctly</td>
        <td>10</td>
      </tr>
      <tr>
        <td>Uses string interpolation for output</td>
        <td>10</td>
      </tr>
      <tr>
        <td>Code compiles and runs without errors</td>
        <td>10</td>
      </tr>
      <tr>
        <td><strong>Total</strong></td>
        <td><strong>100</strong></td>
      </tr>
    </tbody>
  </table>
</div>

---

## Submission

When your program runs correctly and displays the formatted profile:

1. Make sure your name is in the header comment
2. Click **Save** to save your work
3. Click **Submit Lab** below

<div id="lab-submit-container" style="margin: 24px 0;">
  <button id="submit-lab-btn" style="
    background: linear-gradient(135deg, #22c55e 0%, #16a34a 100%);
    color: white;
    border: none;
    padding: 16px 32px;
    border-radius: 8px;
    font-size: 1.1rem;
    font-weight: 600;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 8px;
  ">
    Submit Lab
  </button>
  <div id="submit-status" style="margin-top: 1rem; font-weight: bold;"></div>
  <div id="ai-feedback" style="margin-top: 1rem; padding: 1rem; background: rgba(78, 201, 176, 0.1); border-radius: 8px; display: none;"></div>
  <div id="last-submitted" style="margin-top: 0.5rem; color: #666; font-size: 14px;"></div>
</div>

<script src="/scripts/submit-lab.js"></script>

---

<div class="next-section">
  <p>Next up: <strong>Technical Reflection</strong></p>
  <p>Explain the concepts of string immutability and StringBuilder.</p>
  <a href="/week-04/homework/" class="vscode-button">Continue to Homework →</a>
</div>

<style>{`
  .next-section {
    background: linear-gradient(135deg, rgba(78, 201, 176, 0.15) 0%, rgba(107, 159, 255, 0.15) 100%);
    border: 2px solid #4ec9b0;
    border-radius: 12px;
    padding: 24px;
    text-align: center;
    margin: 32px 0;
  }
  
  .next-section p {
    margin: 8px 0;
    color: rgba(255, 255, 255, 0.9);
  }
  
  .next-section a {
    display: inline-block;
    margin-top: 16px;
  }

  body[data-theme="light"] .next-section {
    background: linear-gradient(135deg, rgba(78, 201, 176, 0.1) 0%, rgba(107, 159, 255, 0.1) 100%) !important;
  }

  body[data-theme="light"] .next-section p {
    color: #333 !important;
  }
  
  details {
    background: rgba(78, 201, 176, 0.1);
    border: 1px solid rgba(78, 201, 176, 0.3);
    border-radius: 8px;
    padding: 12px 16px;
    margin: 8px 0;
  }
  
  details summary {
    cursor: pointer;
    font-weight: 600;
    color: #4ec9b0;
  }
  
  details[open] summary {
    margin-bottom: 12px;
  }
  
  /* Light mode overrides */
  body[data-theme="light"] details {
    background: rgba(37, 99, 235, 0.08) !important;
    border-color: rgba(37, 99, 235, 0.2) !important;
  }
  
  body[data-theme="light"] details summary {
    color: #2563eb !important;
  }
  
  body[data-theme="light"] .styled-table-container table {
    background: #f8fafc !important;
  }
  
  body[data-theme="light"] .styled-table-container th {
    background: #e2e8f0 !important;
    color: #1e293b !important;
  }
  
  body[data-theme="light"] .styled-table-container td {
    color: #334155 !important;
    border-color: #e2e8f0 !important;
  }
  
  body[data-theme="light"] .next-section p {
    color: #1e293b !important;
  }

  body[data-theme="light"] #lab-editor { border-color: #cbd5e1 !important; }
  body[data-theme="light"] #submit-status { color: #1e293b !important; }
  body[data-theme="light"] #ai-feedback { background: rgba(16, 185, 129, 0.12) !important; color: #1e293b !important; border: 1px solid rgba(16, 185, 129, 0.4) !important; }
  body[data-theme="light"] #last-submitted { color: #475569 !important; }
`}</style>
